# --- Étapes 0 & 1 : squelette + upload ---
fastapi>=0.115
uvicorn[standard]>=0.34
jinja2>=3.1
python-multipart>=0.0.18
pydantic-settings>=2.7
python-dotenv>=1.0

# --- Étape 2 & 3 : transcription + résumé (Groq) ---
groq>=1.7

# --- Étape 4 : archivage ---
# Backblaze B2 (API compatible S3) : stockage en ligne principal
boto3>=1.43
# Google Drive : conservé, utilisable en local (facultatif)
google-api-python-client>=2.199
google-auth-oauthlib>=1.4
google-auth-httplib2>=0.4

# --- Étapes suivantes (ajoutées au fur et à mesure) ---
# mega.py                        # étape 4b : archivage MEGA (fichiers volumineux)
# libsql-experimental>=0.1       # étape 6 : bascule SQLite -> Turso (déploiement)
